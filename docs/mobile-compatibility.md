# Mobile compatibility

The read-only documentation service exists to verify whether an independently published MCP-backed integration works in an ordinary ChatGPT Pro conversation on iOS.

The current verification phrase is `saffron-orbit-7f3c2a91`. A mobile check must retrieve this note through `search` and `fetch`, reproduce that phrase exactly, and open the citation page returned by the service.

This check proves publication and read compatibility only. It does not claim access to a private Obsidian vault, write support, automatic tool selection, or direct control over ChatGPT Memory.
